$kind: http-request
url: '{{base_url}}/auth/login'
method: POST
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "email": "abhaydemo1@gmail.com",
      "password": "password123"
    }
scripts:
  - type: afterResponse
    code: |-
      const res = pm.response.json();
      pm.environment.set("token", res.token);
    language: text/javascript
order: 2000
